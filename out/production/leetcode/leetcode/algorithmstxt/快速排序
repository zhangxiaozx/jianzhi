快速排序

基本思想：
1.任取一个元素为中心pivot
2.所有比它小的元素放在pivot前面，比它大的元素放在其后面，形成两个左右子表
3.对两个左右子表再进行步骤1和2的操作
4.直到每个子表的元素只剩一个，排序结束

总结：通过一趟排序，将待排序记录分割成独立的两部分，其中一部分记录的关键字均比另一部分的关键字小，则可分别对这两部分记录进行
排序，以达到整个序列有序。
具体实现：选定一个中间数作为参考，所有元素与之比较，小的调到左边，大的调到右边。

